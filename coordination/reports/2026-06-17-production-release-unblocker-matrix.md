# UAIS Production Release Unblocker Matrix

- Date: 2026-06-17
- Session: S22
- Scope: Redacted production E2E release readiness from local dry-run evidence
- Source evidence:
  - `coordination/reports/2026-06-18-local-production-e2e-smoke.json`
  - `coordination/reports/2026-06-18-local-production-e2e-smoke-dry-run.json`
  - `coordination/reports/2026-06-18-vercel-project-readiness.json`
  - `coordination/reports/2026-06-18-vercel-project-readiness-uais-top.json`
  - `coordination/reports/2026-06-18-vercel-production-deployment-dry-run.json`
  - `coordination/reports/2026-06-18-production-owner-decision-checklist.json`
  - `coordination/reports/2026-06-19-vercel-project-readiness.json`
  - `coordination/reports/2026-06-19-vercel-project-readiness-uais-top.json`
  - `coordination/reports/2026-06-19-production-e2e-release-gate.json`
  - `coordination/reports/2026-06-19-production-owner-decision-checklist.json`
  - `coordination/reports/2026-06-17-local-production-e2e-smoke.json`
  - `coordination/reports/2026-06-17-vercel-env-sync-local-env-dry-run.json`
  - `coordination/reports/2026-06-17-route-smoke-local-env-dry-run.json`
  - `coordination/reports/2026-06-17-teacher-auth-provider-readiness-local-env-dry-run.json`
  - `coordination/reports/2026-06-17-external-storage-smoke-local-env-dry-run.json`
  - `coordination/reports/2026-06-17-external-storage-service-readiness-local-env-dry-run.json`
  - `coordination/reports/2026-06-17-external-storage-production-launch-contract.json`
  - `coordination/reports/2026-06-17-external-storage-container-build-readiness.json`
  - `coordination/reports/2026-06-17-external-storage-reference-service-live-smoke.json`
  - `coordination/reports/2026-06-17-teacher-workflow-ui-smoke.json`
  - `coordination/reports/2026-06-17-teacher-workflow-deployment-smoke-local-env-dry-run.json`
  - `coordination/reports/2026-06-17-teacher-workflow-browser-smoke-local-env-dry-run.json`
  - `coordination/reports/2026-06-18-teacher-workflow-browser-smoke-local-production-live.json` (superseded for current browser proof by the embedded `s22-local-teacher-workflow-browser-smoke` check in `coordination/reports/2026-06-18-local-production-e2e-smoke.json`)
  - `coordination/reports/2026-06-17-production-e2e-release-gate.json`
  - `coordination/reports/2026-06-17-production-e2e-orchestration-plan.json`
  - `coordination/reports/2026-06-17-kangxia-ppt-manual-playback-readiness.md`
  - `coordination/reports/2026-06-17-kangxia-ppt-manual-acceptance-record-template.json`
  - `coordination/reports/2026-06-17-kangxia-ppt-manual-template-gate.json`
  - `coordination/reports/2026-06-17-vercel-link-deploy-readiness.md`
  - `coordination/reports/2026-06-17-vercel-project-readiness.json`
  - `coordination/reports/2026-06-17-teacher-auth-provider-readiness-production-live.json` (expected production evidence slot; currently missing)
  - `coordination/reports/2026-06-17-external-storage-service-readiness-production-live.json` (expected production evidence slot; currently missing)
  - `coordination/reports/2026-06-17-vercel-production-deployment.json` (expected production evidence slot; currently missing)
  - `coordination/reports/2026-06-17-learning-ppt-playback-deployment-smoke-production-live.json` (expected deployed student playback evidence slot; currently missing)
- Redaction rule: This report records variable names, responsible sessions, and present/missing status only. No credential values, cookies, response bodies, local private paths, provider voice IDs, raw audio, or deployment URLs are included.

## Current Gate Status

| Requirement | Current evidence status | Release status | Next proof needed |
| --- | --- | --- | --- |
| Website teacher workflow UI | `accepted` local UI smoke with `target: "teacher-workflow-ui-smoke"` and explicit `feature-evidence-passed` markers for voice upload/select, `uploadedSampleAudioPayload: true` proving the selected audio file is read and submitted to `/api/ai/voice-sample`, `voiceSampleDurationGate: true` proving browser metadata duration gating before registration, `selectedSampleIdentity: true` voice-sample identity propagation, `/api/ai/session` signed-session bootstrap, `authFailClosed: true` handling when signed AI session minting is denied, `serverWorkflowStatus: true` for the `/api/ai/teacher-ppt-workflow` server-state bridge, preflight, voiceRef, PPT WAV downloads, and step gating; the `/teaching` page now renders an explicit `Kang Xia PPT 19 页` workflow marker, a `使用 Kang Xia 10 秒声音` owner-provided voice selector, a `刷新服务端 workflow` action, blocks downstream protected workflow calls when `/api/ai/session` denies the teacher session, reads server-owned workflow status/download metadata with same-origin credentials, sends uploaded 10-second voice audio as `sampleAudioBase64` in the local component acceptance path, and sends 19 slide scripts before showing 19 per-slide WAV download links. `/api/ai/voice-sample` now also verifies parseable uploaded WAV payload duration before storage/provider submission, so a short WAV cannot pass merely by claiming `sampleDurationSeconds: 10`. The aggregate gate now lists `uploadedSampleAudioPayload`, `voiceSampleDurationGate`, `selectedSampleIdentity`, `authFailClosed`, and `serverWorkflowStatus` in `requiredFeatures` and rejects stale `accepted` JSON without those marker proofs | Satisfied for local UI | Replace or supplement with deployed browser smoke when a deployment URL exists. |
| Local production E2E diagnostic | Fresh 2026-06-18 `local-production`/`passed`; build, localhost external storage reference service, `next start`, `/learning` student PPT playback smoke, `/teaching` rendered anchor smoke, real browser workflow smoke, and protected AI routes passed with fixture env. The new learning playback check proves `/learning` HTTP 200, the published Kang Xia manifest for `elementary-math-research` returns 19 slides with `康霞博士克隆声音`, the manifest remains student-safe/redacted, and the first published slide audio returns `audio/wav` with `RIFF/WAVE` headers. The local diagnostic harness and aggregate production gate both require the first-slide audio evidence to carry at least 1024 bytes, so a header-only WAV cannot count as student playback proof. The browser check now proves short-upload `voiceSampleDurationGate: "passed"`, 10-second voice file selection, fixture-only signed-session bootstrap, preflight, voiceRef, PPT narration submit, `pptNarrationSlidePayload: "passed"` proving the UI submits 19 ordered slide scripts to `/api/ai/ppt-narration`, 19 per-slide WAV download links, and `perSlideWavDownloadHrefContract: "passed"` proving those links target protected `/api/ai/ppt-narration/audio/...` routes inside the orchestrated local production diagnostic. The protected route chain still proves signed trusted issuer proof -> hardened issuer cookie -> teacher AI session with redacted `authProviderContract: present` -> teacher ownership summary -> teacher PPT workflow, with teacher ownership and Qwen lifecycle audit configured through external storage backends. | Diagnostic only | Does not satisfy production release gates because it is localhost/local-loopback evidence with fixture API interception; rerun after route/storage/UI changes to catch local production regressions. |
| Deployed teacher workflow page | `dry-run-blocked`; current evidence environment is `unspecified`, `deploymentOrigin.originClass: "missing"`, and deployment fingerprint is missing; required anchors now include voice upload, voice select, `uploadedSampleAudioPayload`, `voiceSampleDurationGate`, `selectedSampleIdentity`, signed-session bootstrap, `workflowSessionActions` for the four protected teacher workflow calls, `serverWorkflowStatus`, `serverWorkflowProgress` for the signed-auth/provider/storage/route readiness chain, redacted deployment fingerprint evidence, rendered-page fingerprint evidence, redacted Vercel production deployment binding, and redacted remote-HTTPS origin proof; the aggregate gate now rejects stale live/passed page evidence unless it comes from `teacher-workflow-deployment-smoke`, proves those rendered anchors including the `data-uais-voice-sample-duration-gate="browser-metadata"` and `data-uais-server-workflow-progress="auth-provider-storage-route"` markers, carries both the deployment fingerprint and rendered HTML fingerprint, reports `deploymentOrigin.originClass: "remote-https"`, and carries `vercelProductionDeploymentEvidence.status: "matched"` with `deploymentObservationStatus: "observed"`. When Vercel production deployment evidence is live/deployed, the aggregate gate also compares the page-smoke deployment fingerprint to the Vercel production fingerprint and blocks mismatches with `vercel-production-deployment-fingerprint-mismatch`; missing embedded Vercel binding now blocks with `deployed-teacher-workflow-page-vercel-deployment-binding-not-proven`. Release-run consistency now refuses to count deployed teacher page evidence as `present` unless the same target, required anchors, deployment fingerprint, rendered-page fingerprint, remote-HTTPS origin, non-mismatching Vercel deployment fingerprint proof, and matched/observed Vercel deployment binding are present. The page smoke harness itself now also blocks `--environment production` before fetching `/teaching` when the deployment origin is local, private, insecure HTTP, invalid, otherwise not `remote-https`, or when Vercel production deployment evidence is missing/stale/mismatched. | Blocked | Approved live `/teaching` smoke with `UAIS_DEPLOYMENT_BASE_URL`, `--environment production`, `--release-run-id <release-run-id>`, and `--vercel-production-deployment <vercel-production-deployment-evidence>`, including the voice select, uploaded audio payload marker, browser metadata duration-gate marker, selected sample identity, signed-session bootstrap marker, protected workflow action manifest, server workflow status marker, server workflow readiness-progress marker, a deployment fingerprint matching the Vercel production deployment evidence, rendered-page fingerprint, matched/observed Vercel binding, and remote-HTTPS deployment origin proof. Use `--environment local-production` for localhost production-build diagnostics. |
| Deployed teacher workflow browser interaction | `waiting-for-deployed-page` in the aggregate gate; dry-run browser smoke is currently blocked by missing `UAIS_DEPLOYMENT_BASE_URL`. The browser smoke uses Playwright only in live mode, opens deployed `/teaching`, fixture-intercepts workflow APIs, first selects a 1-second WAV and proves the browser metadata gate disables teacher voice registration, then selects a 10-second WAV, refreshes server workflow, requests signed-session bootstrap, clicks voice sample/preflight/voiceRef/PPT narration actions, verifies the PPT narration POST includes 19 ordered slide scripts, verifies 19 per-slide WAV download links, and verifies the redacted href contract for protected `/api/ai/ppt-narration/audio/...` routes without calling remote provider or storage mutation routes. Current project dependency evidence still reports `npm ls playwright --depth=0` as empty, so production execution must either install Playwright as an approved dev dependency or use the documented transient runtime command. The orchestrated `local-production` E2E diagnostic now includes this browser smoke and records `voiceSampleDurationGate`, signed-session bootstrap, preflight, voiceRef, PPT narration submit, `pptNarrationSlidePayload`, per-slide WAV download links, and `perSlideWavDownloadHrefContract` as `passed`; the production gate now rejects stale browser-smoke evidence unless those interaction results are present and `vercelProductionDeploymentEvidence.status: "matched"` / `deploymentObservationStatus: "observed"` is proved. Release-run consistency also refuses to count browser smoke unless target identity, required interactions, matching deployment fingerprint, remote-HTTPS origin, fixture-blocked mutation policy/live workflow API policy, deployed-page readiness, and redacted Vercel deployment binding are all proved. This local-loopback evidence proves UI interaction wiring but does not satisfy the production gate because it is not `environment: "production"` and does not use a remote-HTTPS deployment origin. The browser smoke harness itself also blocks `--environment production` before browser navigation when the deployment origin is not `remote-https`, now blocks when Vercel production deployment evidence is missing, and blocks before browser launch when supplied Vercel production deployment evidence is stale, has a mismatched release-run id, is not observed, or carries a different redacted deployment fingerprint. | Waiting on deployed page and production browser evidence | After the deployed page smoke is production/live/passed, prepare Playwright either as a project-local resolvable module using `runtimeSetup.packageInstallCommand` or with the explicit `runtimeSetup.transientRuntimeCommand`; install Chromium if needed with `runtimeSetup.browserInstallCommand`, then run against the real remote-HTTPS deployment URL with `--vercel-production-deployment <vercel-production-deployment-evidence>`. Aggregate gate then requires production/live/passed browser evidence with matching deployment fingerprint, remote-HTTPS origin, fixture-only API policy, redacted Vercel production deployment binding, and all browser interaction results `passed`, including `voiceSampleDurationGate`, `pptNarrationSlidePayload`, and `perSlideWavDownloadHrefContract`. |
| Deployed student PPT playback | Waiting for Vercel production deployment; the aggregate gate now has requirement id `deployed-learning-ppt-playback`. Before production deployment evidence is live/deployed, this requirement records `waiting-for-production-deployment` instead of adding a false blocker. After deployment exists, the gate requires live production evidence from `target: "learning-ppt-playback-deployment-smoke"`, `deploymentOrigin.originClass: "remote-https"`, matching deployment fingerprint, HTTP 200 proof for `/learning`, the playback manifest, and the first-slide audio request, a student-safe manifest for `elementary-math-research`, `audioManifestId: "audio-manifest-elementary-math-research-natural-number-ordinal-theory-ppt1"`, `teacherName: "康霞博士"`, `voiceLabel: "康霞博士克隆声音"`, exactly 19 slides, first title `自然数的序数理论`, last title `作业布置`, first audio URL exactly `/api/learning/ppt-playback/audio/audio-manifest-elementary-math-research-natural-number-ordinal-theory-ppt1/tts_natural-number-ordinal-theory-ppt1_slide-01`, first-slide WAV `RIFF/WAVE` header plus at least 1024 bytes of audio content-length proof, and redacted `vercelProductionDeploymentEvidence.status: "matched"` / `deploymentObservationStatus: "observed"` proof from the Vercel production deployment evidence. Release-run consistency now also refuses to count this evidence as `present` unless that same target, remote-HTTPS origin, deployment fingerprint, Vercel deployment binding, three HTTP 200 statuses, Kang Xia manifest contract, exact first-slide audio URL contract, and WAV header/length contract are all proved, so a weak live/passed JSON cannot join the final release-run binding. | Waiting on production deployment and deployed student playback evidence | After the Vercel production deployment URL exists, run `node scripts/learning-ppt-playback-deployment-smoke.mjs --live --approved --environment production --base-url <deployment-url> --env-file .env.local --release-run-id <release-run-id> --vercel-production-deployment <vercel-production-deployment-evidence>` and save `coordination/reports/2026-06-17-learning-ppt-playback-deployment-smoke-production-live.json`; the final gate must compare its deployment fingerprint and matched Vercel production deployment binding before approval. |
| Vercel env placement | `dry-run`; current env-sync evidence reports `authProviderMode: "missing"`, `externalStorageEndpoint.endpointClass: "missing"`, and redacted `secretStrength` checks for production-critical secrets as `missing`. Apply mode now requires explicit owner approval, linked project evidence, ready Vercel project-readiness evidence before any `vercel env add` call, aggregate gate evidence `projectReadinessEvidenceStatus: "ready"`, `target: "vercel-env-sync"` identity proof, explicit `authProviderMode` proof, `targets` coverage for both production and preview, redacted `applyPreflight.status: "passed"` proof from the mutation-safety preflight, redacted `applySummary.status: "applied"` proof with nonzero production/preview action counts, a local-only skipped-entry count, `valuesRedacted: true`, and `cliOutputOmitted: true`, local-only OIDC smoke env exclusion proof, selector-specific auth env, redacted sufficient `secretStrength` proof for `UAIS_AI_ACCESS_SIGNING_SECRET`, `UAIS_TEACHER_AUTH_SESSION_SIGNING_SECRET`, `UAIS_EXTERNAL_STORAGE_ACCESS_TOKEN`, plus `UAIS_TEACHER_AUTH_ISSUER_SECRET` when `trusted-cookie-issuer` is selected, and the same non-secret `releaseRunId` as the rest of the production evidence chain. Env-sync evidence must also carry redacted `externalStorageEndpoint` with `endpointClass: "remote-https"` plus a redacted `externalStorageServiceFingerprint` derived from `UAIS_EXTERNAL_STORAGE_BASE_URL`; missing fingerprint proof now blocks `vercel-env-placement` itself with `vercel-env-external-storage-fingerprint-not-proven`, and the final gate later compares that fingerprint with storage readiness and storage smoke evidence so Vercel cannot point at a different external storage origin. If `oidc-jwks` is selected, env-sync evidence must carry redacted `oidcEndpointSecurity` with both issuer and JWKS classified as `remote-https`; localhost, private-network, HTTP, invalid, weak, or missing endpoint/secret classes cannot satisfy the aggregate gate. Release-run consistency now also refuses to count Vercel env-sync as `present` unless both the redacted apply preflight and apply summary are proved, so an otherwise well-shaped apply JSON cannot bind the final release run without mutation-safety and execution-count evidence. | Blocked | Choose the production teacher-auth selector, then run approved `vercel env add` apply to production and preview only after Vercel project readiness is `ready`, `localOnlySmokeEnvNotSynced` is proved, all production-critical `secretStrength` checks are `sufficient`, the external storage base URL classifies as `remote-https` and emits a matching redacted storage-origin fingerprint, any OIDC/JWKS endpoints classify as `remote-https`, the apply output records redacted preflight plus action counts while omitting CLI output, and the command includes the approved `--release-run-id <release-run-id>`. |
| Vercel project link | Fresh 2026-06-19 `local-blocked`; project-local Vercel CLI is present, redacted authenticated CLI account evidence is present, but current team scope evidence is now missing (`teamCount: 0`), `.vercelignore` passes, exact project-candidate evidence is still missing for both `uais` and `uais-top` with 0 filtered and 0 exact matches, and the link marker is missing. The readiness script can accept an owner-approved `--project-id` as redacted project-candidate evidence without printing the ID; the aggregate gate still requires current team scope, project candidate, and project link marker before readiness can satisfy the project gate, and it rejects otherwise-ready Vercel project-readiness JSON unless the evidence proves value redaction plus project/org/account/team/deployment/local-path omission safety. | Blocked | Owner/S22 must first select or confirm the current Vercel team scope, then choose/create the intended Vercel project and rerun `scripts/vercel-project-readiness.mjs --project-name <approved-name>` or `--project-id <approved-project-id>`, followed by owner-approved `npx vercel link`. |
| Owner decision checklist | Fresh 2026-06-19 `owner-decisions-required`; a redacted machine checklist now groups the remaining production blockers into owner/S22 project selection, S12/S19 teacher-auth provider selector/env, S12/S19/S24 external storage service/env, S19/S22 env/deploy/smoke chain, S24 manual PPT playback, and final release-run binding. The checklist confirms local production diagnostics are passed but release-ineligible, records `evidenceFreshness: "current"`, requires the current local production check set including `s22-local-learning-ppt-playback-smoke` and `s22-local-teacher-workflow-browser-smoke`, and records `browserProofStatus: "passed"` with no missing browser results. It now keeps `vercel-team-scope-missing` attached to the `vercel-project-selection` owner decision instead of leaving it only in final release-run blockers, and its safe next actions begin with `select-or-confirm-current-vercel-team-scope`. It also explicitly requires `production-session-cookie-pair-contract`, `trusted-cookie-session-issuer-separation-if-selected`, `oidc-jwks-signing-key-readiness-if-oidc-selected`, `teacher-auth-provider-specific-readiness-proof`, and `teacher-auth-provider-redaction-safety` for the teacher-auth decision, plus `production-launcher-env-contract`, `container-runtime-artifact`, `container-build-readiness-evidence`, `production-service-mode-health-target`, `service-api-contract-version-proof`, `storage-readiness-vercel-env-sync-binding`, `external-storage-service-fingerprint-consistency`, `external-storage-smoke-readiness-binding`, `external-storage-response-shape-contract`, and `external-storage-redaction-safety` for the external-storage service decision; the deploy/smoke proof list includes `deployed-learning-ppt-playback-smoke` so student consumption of Kang Xia audio is not lost behind teacher workflow proof. It preserves production deployment-binding blockers for browser smoke, learner playback, protected route smoke, and manual PPT playback inside owner decisions, keeping final release-run binding in `waiting-for-upstream-evidence` until every required item is bound to the same observed Vercel production deployment evidence. It carries only statuses/counts/required decisions rather than URLs, project ids, credentials, or local paths. | Coordination aid only | Use `coordination/reports/2026-06-19-production-owner-decision-checklist.json` before starting live actions so Vercel team/project/link, env apply, deploy, deployed student playback smoke, storage smoke, and manual QA happen in the right order with owner approval. |
| Teacher auth provider readiness | `dry-run-blocked`; local-env dry-run evidence now carries a non-secret release-run id and a redacted two-cookie production session contract, but reports `authProviderMode: "missing"`, missing/insufficient teacher-auth session signing-secret proof, and `teacher-auth-provider-readiness-not-live-ready` at the aggregate gate. Accepted evidence must come from `target: "teacher-auth-provider-readiness"`, `mode: "live"`, `environment: "production"`, `status: "ready"`, an accepted provider selector (`trusted-cookie-issuer` or `oidc-jwks`), a same-run `vercelEnvSyncEvidence.status: "matched"` guard from `--vercel-env-sync <vercel-env-sync-evidence>`, explicit `vercelEnvSyncEvidence.applyPreflight: "proved"` from the approved env-sync mutation-safety preflight, explicit `vercelEnvSyncEvidence.releaseRunIdStatus: "matched"` proof, sufficient teacher-auth session signing-secret strength, hardened session cookie contract proof, exact production cookie pair proof for `uais_teacher_auth_claims` and `uais_teacher_auth_signature`, provider-specific proof for either trusted issuer secret/issuer-proof TTL plus separate session/issuer secret values, or remote-HTTPS OIDC issuer/JWKS plus audience and teacher-id claim, redaction safety proving secrets/provider URLs/response bodies/local paths/cookie values are omitted, and the same non-secret `releaseRunId` as the rest of the production evidence chain. The readiness harness now blocks production live runs before reusable ready evidence when Vercel env-sync evidence is missing, not applied, missing passed apply-preflight proof (`vercel-env-sync-apply-preflight-not-proven`), release-run mismatched, or carries a different `authProviderMode`. The aggregate gate now rejects stale teacher-auth readiness JSON that omits the deploy-env preflight binding with `teacher-auth-provider-vercel-env-sync-not-proven`, rejects same-shape readiness JSON that omits same-run env-sync proof with `teacher-auth-provider-vercel-env-sync-release-run-not-proven`, and release-run consistency refuses to count teacher-auth readiness as `present` unless the same aggregate readiness contract is satisfied; weak `live/production/ready` JSON cannot bind the final release run. The readiness harness itself now blocks `--live --approved` evidence outside `--environment production` with `teacher-auth-provider-readiness-not-production`, so preview/local live checks cannot produce reusable ready evidence before the aggregate gate sees them. If `trusted-cookie-issuer` is selected, live readiness and the aggregate gate now require `trustedIssuerContract.sessionIssuerSecretSeparation: "proved"` so a shared session/issuer secret cannot satisfy production readiness; the runtime provider contract also emits redacted `trustedIssuerSeparation` and blocks production readiness with `shared-UAIS_TEACHER_AUTH_SESSION_AND_ISSUER_SECRET` when the two configured values match. If `oidc-jwks` is selected, live production readiness now requires the fetched JWKS to contain at least one usable RS256/RSA signing public key and the aggregate gate rejects stale ready JSON unless `oidcJwksReadiness.signingKeys: "present"` is proved. The aggregate gate now also compares the auth selector reported by Vercel env-sync, teacher-auth provider readiness, and protected route smoke; once all three are present, any selector mismatch blocks with `teacher-auth-provider-selector-mismatch`. | Blocked | After S12/S19 choose the production provider and S19 places server-only env, run `node scripts/teacher-auth-provider-readiness.mjs --live --approved --environment production --env-file <env-file> --release-run-id <release-run-id> --vercel-env-sync <vercel-env-sync-evidence>` before protected deployment route smoke, then ensure env-sync evidence has passed `applyPreflight` plus applied `applySummary`, includes matching `releaseRunId`, env-sync and route-smoke evidence carry the same selector, trusted-cookie evidence has `sessionIssuerSecretSeparation: "proved"` plus the exact two-cookie production contract, and OIDC evidence has `oidcJwksReadiness.signingKeys: "present"`. |
| External storage service readiness | `dry-run-blocked`; a redacted production launcher dry-run contract is now present and proves the intended env-only secret launch shape, forced production service mode, and persistent data-dir requirement, but local-env readiness still has missing external storage base URL, missing service-origin fingerprint, and no live `/healthz` proof, so the aggregate gate reports `external-storage-service-readiness-not-live-ready`. Accepted evidence must come from `target: "external-storage-service-readiness"`, `mode: "live"`, `environment: "production"`, `status: "ready"`, `storageEndpoint.endpointClass: "remote-https"`, redacted `storageServiceFingerprint` proof derived from the service origin, production service identity proof from `/healthz` including `target: "uais-external-storage-production-service"`, API contract version proof from `/healthz`, durable backing-store readiness, redacted health proof, safety proof that service URL/response bodies/local paths/values are omitted, a same-run `vercelEnvSyncEvidence.status: "matched"` guard from `--vercel-env-sync <vercel-env-sync-evidence>`, explicit `vercelEnvSyncEvidence.applyPreflight: "proved"` from the approved env-sync mutation-safety preflight, explicit `vercelEnvSyncEvidence.releaseRunIdStatus: "matched"` proof, and the same non-secret `releaseRunId` as the rest of the production evidence chain. The readiness harness now blocks production live runs before `/healthz` when Vercel env-sync evidence is missing, not applied, missing passed apply-preflight proof (`vercel-env-sync-apply-preflight-not-proven`), release-run mismatched, missing the redacted external-storage fingerprint, or points at a different storage-service origin. The aggregate gate now blocks otherwise live-ready production service-readiness JSON that omits the redacted service-origin fingerprint with `external-storage-service-readiness-fingerprint-not-proven`, blocks stale readiness JSON that omits the env apply-preflight binding with `external-storage-service-vercel-env-sync-not-proven`, and blocks stale same-shape readiness JSON that omits same-run env-sync release-run proof with `external-storage-service-vercel-env-sync-release-run-not-proven`, before later storage consistency compares readiness/smoke/Vercel env fingerprints. The reference service and production smoke health check now also require `apiContractVersion: "uais-external-storage-v1"`, and the aggregate gate rejects stale service-readiness JSON unless `health.apiContractVersion: "matched"` is proved. This prevents the production chain from treating a contract-only smoke harness, stale health endpoint, local launcher contract, local reference service, or unbindable readiness JSON as a provisioned compatible production storage service. | Blocked | After S12/S24/S19 provision a real remote-HTTPS external storage service using the launcher contract, run `node scripts/external-storage-service-readiness.mjs --live --approved --environment production --env-file <env-file> --release-run-id <release-run-id> --vercel-env-sync <vercel-env-sync-evidence>` and save the redacted evidence before write/read storage smoke; the supplied env-sync evidence must include passed `applyPreflight`, applied `applySummary`, matching `releaseRunId`, and the matching redacted storage-origin fingerprint. |
| Vercel production deployment | Fresh 2026-06-18 dry-run evidence is `dry-run-blocked`; the aggregate gate now has requirement id `vercel-production-deployment` and currently blocks with `vercel-production-deployment-not-proven` while reporting `deploymentObservationStatus: "missing"`. The deployment evidence harness refuses approved live deploy before Vercel CLI invocation unless ready `vercel-project-readiness` evidence and applied `vercel-env-sync` evidence are supplied; when a deployment `--release-run-id` is provided, the harness also blocks env-sync evidence whose `releaseRunId` is missing or different, and now blocks applied env-sync evidence that lacks redacted external-storage fingerprint proof with `vercel-env-sync-external-storage-fingerprint-not-proven`, lacks the redacted apply execution summary with `vercel-env-sync-apply-summary-not-proven`, or lacks passed mutation-safety preflight proof with `vercel-env-sync-apply-preflight-not-proven`. The refreshed orchestration plan passes both `--vercel-project-readiness <evidence>` and `--vercel-env-sync <evidence>` into the deploy command and records `ready-project-readiness-guard`, `applied-env-sync-guard`, `env-sync-storage-fingerprint-guard`, `env-sync-apply-summary-guard`, and `env-sync-apply-preflight-guard`. The aggregate gate now rejects stale deployed evidence unless the Vercel deployment evidence includes `projectReadinessGuard: "proved"` via prerequisite `s22-vercel-project-readiness: "ready"`, `envSyncGuard: "proved"` via prerequisite `s19-vercel-env-sync-apply-evidence: "applied"`, explicit `envSyncApplyPreflightGuard: "proved"` from `vercel-env-sync.applyPreflight`, redacted deployment observation proof with `deploymentObservation.status: "observed"`, a valid observed timestamp and non-empty source, and redaction safety including `projectReadinessEvidencePathOmitted` and `envSyncEvidencePathOmitted`. Missing explicit deploy-level preflight binding now blocks with `vercel-production-deployment-env-sync-apply-preflight-not-proven`, so older live/deployed JSON cannot satisfy deployment or release-run readiness. Accepted evidence must come from `target: "vercel-production-deployment"`, `mode: "live"`, `environment: "production"`, `status: "deployed"`, carry a redacted deployment fingerprint, classify `deploymentOrigin.originClass` as `remote-https`, prove the project-readiness guard, prove the env-sync apply guard from the same release run with storage-origin fingerprint proof plus redacted apply-preflight and apply-summary proof, prove deployment observation, and prove redaction safety for omitted deployment URL(s), token, project/org/account/team identifiers, project-readiness evidence path, env-sync evidence path, and local private paths. The final release-run consistency helper now also treats Vercel deployment evidence as waiting until all of those deployment proof details, including explicit env-sync apply-preflight guard proof and observation proof, are present; a bare `live/deployed/production` status is no longer enough to mark the deployment run id present. This fingerprint becomes the canonical production deployment fingerprint for deployed page smoke; mismatches block the page requirement with `vercel-production-deployment-fingerprint-mismatch`. | Blocked | After owner-approved project/scope/token path, ready Vercel project-readiness/link evidence, and S19 env apply evidence that covers production and preview with the same release-run id, redacted storage-origin fingerprint proof, redacted apply-preflight proof, and redacted apply-summary proof, create or inspect the Vercel production deployment and write redacted evidence with explicit `envSyncApplyPreflightGuard`, deployment observation proof, and redaction safety at `coordination/reports/2026-06-17-vercel-production-deployment.json` before running deployed page/browser/route/storage smokes against the same deployment fingerprint. |
| Production release-run binding | Waiting; the aggregate gate now has requirement id `production-release-run-consistency`. It waits until Vercel env apply, Vercel production deployment, deployed page smoke, deployed browser smoke, deployed learner PPT playback smoke, teacher-auth provider readiness, external-storage service readiness, protected route smoke, production external storage smoke, and manual PPT acceptance are all production/live or accepted. The final release-run list now always includes `learningPptPlayback`; if deployed `/learning` Kang Xia playback evidence is absent after the rest of the chain is present, release-run consistency remains `waiting` instead of reporting `match: true` with the learner proof omitted. Once all are present, each evidence file must carry the same `releaseRunId`; missing values block with `production-release-run-id-missing` and mismatches block with `production-release-run-id-mismatch`. The final gate redacts run-id values and records only present/missing/match status. | Waiting on production evidence | Choose one non-secret `<release-run-id>` for the approved release attempt and pass `--release-run-id <release-run-id>` to Vercel env apply, teacher-auth readiness, external-storage service readiness, Vercel deployment evidence, deployed page smoke, browser smoke, deployed learner PPT playback smoke, route smoke, external storage smoke, and PPT acceptance commands. |
| Protected deployment route smoke | `dry-run-blocked`; route inventory now requires `deployment-route-smoke` target identity, explicit `authProviderMode` proof for `trusted-cookie-issuer` or `oidc-jwks`, provider-matched issuer route auth (`signed-admin-ai-access` for trusted issuer, `oidc-jwks-bearer-token` for OIDC/JWKS), conditional trusted-issuer or OIDC/JWKS teacher-auth issuance, hardened teacher auth cookies with bounded Max-Age, API response-shape proof for the teacher-auth issuer body (`teacherAuthSession`, redacted `authProviderContract`, and `s12TeacherAuthIssuerBoundary`), the teacher AI session including redacted `authProviderContract`, teacher ownership summary, and teacher PPT workflow routes, plus a redacted deployment fingerprint matching the deployed page smoke and `deploymentOrigin.originClass: "remote-https"`. The teacher PPT workflow response-shape proof now must include `workflowReadyForDownloads`, `workflowDownloadContract`, `workflowAudioDownloadPattern`, and `workflowExportDownloadUrl`, so the protected production route smoke cannot pass unless `/api/ai/teacher-ppt-workflow` exposes the redacted export/WAV download contract needed by the teacher UI. Release-run consistency now refuses to count route smoke as `present` unless this same route-smoke contract is satisfied, the deployed page/Vercel deployment chain is already production-ready, teacher-auth readiness binding is matched, and route-smoke evidence carries redacted `vercelProductionDeploymentEvidence.status: "matched"` / `deploymentObservationStatus: "observed"` proof. The live route-smoke harness itself now blocks `--environment production` before any protected route request when the deployment origin is local, private, insecure HTTP, invalid, or otherwise not `remote-https`, now blocks when Vercel production deployment evidence is missing, and blocks before protected API requests when supplied Vercel production deployment evidence is stale, has a mismatched release-run id, is not observed, or carries a different redacted deployment fingerprint. For `oidc-jwks`, the route-smoke plan also emits redacted `oidcEndpointSecurity` and blocks `--environment production` with `production-oidc-endpoints-not-remote-https` unless issuer and JWKS endpoints both classify as `remote-https`. | Blocked | Approved live route smoke after deployment URL, signing/auth env, teacher-auth readiness evidence, and a safe OIDC smoke token if `oidc-jwks` is selected, against the same remote-HTTPS deployment origin as the page smoke and remote-HTTPS OIDC issuer/JWKS endpoints, with `--release-run-id <release-run-id> --vercel-production-deployment <vercel-production-deployment-evidence> --teacher-auth-provider-readiness <teacher-auth-provider-readiness-evidence>`. Confirm the teacher-auth issuer route reports its redacted session/provider/S12 boundary proof and the teacher PPT workflow route reports ready-for-downloads plus redacted export/WAV download pattern proof. Use `--environment local-production` for localhost production-build diagnostics. |
| External durable storage smoke | `dry-run-blocked`; local reference service smoke is `local-reference`/`passed` only and now proves health with redacted durable backing-store `write-read-delete` readiness, ownership merge/read-after-merge, lifecycle audit append/read-after-append, same-run smoke marker readback, and `storageEndpoint.networkClass: "local-loopback"` / `storageEndpoint.endpointClass: "local-loopback"` locally; aggregate gate now requires `target: "external-storage-smoke"` identity proof, `storageEndpoint.networkClass: "remote"` plus `storageEndpoint.endpointClass: "remote-https"` for production evidence, redacted `storageServiceFingerprint` proof from the same service origin as readiness evidence, redacted `externalStorageServiceReadinessEvidence.status: "matched"` plus `externalStorageServiceReadinessEvidence.releaseRunIdStatus: "matched"` binding from `--external-storage-service-readiness <external-storage-service-readiness-evidence>`, and `storageResponseShapes` proof for health including `durableBackingStore`, ownership merge/read, lifecycle audit append/read, `runScopedSmokeGrant`, and `runScopedSmokeAuditEvent` checks. Release-run consistency now also refuses to count storage smoke as `present` unless that same production storage-smoke contract is proved, so a bare `live/production/passed` JSON with a release-run id cannot join final approval. The `external-storage-service-consistency` gate now waits until storage readiness proves remote HTTPS, production identity, API contract version, durable backing-store readiness, redaction safety, and until storage smoke proves every required response shape, remote HTTPS endpoint classification, and matched same-run readiness binding; only then does it compare redacted fingerprints. When fully applied Vercel env-sync evidence is present, the same consistency gate also requires its `externalStorageServiceFingerprint` to match. Missing or mismatched storage-service fingerprints block with `external-storage-service-fingerprint-missing` or `external-storage-service-fingerprint-mismatch`; weak env/readiness/smoke evidence no longer participates in fingerprint comparison. The live smoke harness itself now also blocks `--environment production` before any network write/read when external-storage service-readiness evidence is missing, stale, not production/live/ready, release-run mismatched, carries a different redacted service fingerprint, or when the endpoint is local, private, insecure HTTP, invalid, or otherwise not `remote-https`. The aggregate gate additionally rejects stale storage-smoke JSON whose readiness binding has the right target/status but omits same-run release proof with `external-storage-smoke-service-readiness-release-run-not-proven`. | Blocked | Approved live write/read smoke against the same production external storage service already proven by Vercel env apply and readiness, with `--environment production` and `--external-storage-service-readiness <external-storage-service-readiness-evidence>`, proving remote HTTPS endpoint classification, matched same-run readiness binding, matching redacted service fingerprint, health backing-store readiness, and same-run ownership/lifecycle audit markers without printing endpoint values or marker values. Use `--environment local-reference` for localhost reference-service checks. |
| PowerPoint/WPS playback acceptance | `record-blocked`; desktop apps and narrated PPTX are ready for human QA, machine preflight is `passed`, the current template record now proves `manualRecordPackageIdentityStatus: "matched"` and `manualRecordArtifactFingerprintStatus: "matched"` for the Kang Xia narrated PPTX artifact, and the aggregate gate records 19 expected slide checks. The refreshed template now pre-binds each slide result to its expected `slideId` and deterministic `audioId`, and the manual acceptance script rejects otherwise complete records when a slide is checked under the wrong slide/audio id or when `heardTargetVoice` is not true for the target Kang Xia cloned voice. The refreshed gate also carries `manualRecordConfirmationStatus: "template-not-accepted"` and `manualRecordReleaseRunStatus: "missing"`; it rejects otherwise complete accepted JSON unless the final manual record sets `manualRecordConfirmationStatus: "accepted-after-human-playback"` and proves `manualRecordReleaseRunStatus: "matched"`. The manual acceptance script and aggregate gate reject release-run-bound manual acceptance unless the record is bound to the expected redacted production deployment fingerprint with `manualRecordDeploymentFingerprintStatus: "matched"` and proves `manualRecordAfterDeploymentStatus: "proved"` so playback QA happened after the deployment being approved. A release-run-only manual record cannot produce accepted final evidence. The aggregate production gate now also rejects otherwise accepted PPT manual playback evidence unless it reports `deploymentEvidenceSource: "vercel-production-deployment"` and `deploymentObservationBindingStatus: "proved"`, so lower-level command-option bindings remain diagnostic only and cannot satisfy final approval. The remaining template evidence still has `manualRecordEvidenceStatus: "incomplete"`, `manualRecordTimingStatus: "missing"`, `manualRecordAfterDeploymentStatus: "missing"`, no tester timestamp, no accepted applications, no release-run binding, no deployment-fingerprint binding, and no explicit human-playback confirmation. Accepted manual playback evidence requires actual human PowerPoint and WPS playback bound to the same production release run, deployment fingerprint, slide/audio ids, target Kang Xia cloned voice, and Vercel production deployment evidence source, not just package/artifact identity. | Blocked | Complete the manual acceptance record with the same non-secret `releaseRunId` used by deployment/smoke evidence, the redacted deployment fingerprint for that release, tester, non-future valid tested-at timestamp, PowerPoint/WPS versions, `status: "accepted-after-human-playback"`, `manualRecordAfterDeploymentStatus: "proved"`, and slide 1-19 `audioPlays: true` plus `heardTargetVoice: true` only after human playback of the prefilled slide/audio id in both Microsoft PowerPoint and WPS Presentation, then generate final evidence via `--vercel-production-deployment <evidence>` rather than diagnostic command-option bindings. |

## Local Env Readiness Snapshot

| Variable | Redacted local status | Production role | Release implication |
| --- | --- | --- | --- |
| `UAIS_LIVE_AI_APPROVAL_TOKEN` | Present | Live provider approval | Available locally, still needs Vercel placement if required for deployment. |
| `DEEPSEEK_API_KEY` | Present | DeepSeek text reasoning | Provider key appears locally ready for Vercel sync. |
| `DEEPSEEK_BASE_URL` | Present | DeepSeek endpoint | Provider endpoint appears locally ready for Vercel sync. |
| `DEEPSEEK_MODEL` | Present | DeepSeek model | Provider model appears locally ready for Vercel sync. |
| `DASHSCOPE_API_KEY` | Present | Qwen voice/PPT/multimodal | Provider key appears locally ready for Vercel sync. |
| `DASHSCOPE_BASE_URL` | Present | Qwen endpoint | Provider endpoint appears locally ready for Vercel sync. |
| `QWEN_MULTIMODAL_MODEL` | Present | Qwen multimodal | Provider model appears locally ready for Vercel sync. |
| `QWEN_IMAGE_MODEL` | Present | Qwen image | Provider model appears locally ready for Vercel sync. |
| `QWEN_TTS_MODEL` | Present | Qwen voice/PPT narration | Provider model appears locally ready for Vercel sync. |
| `UAIS_AI_ACCESS_SIGNING_SECRET` | Missing | Signed AI access | Blocks protected route smoke and production AI access. |
| `UAIS_TEACHER_AUTH_PROVIDER` | Missing | Trusted teacher auth provider selector | Blocks production teacher session issuance. Implemented selectors are now `trusted-cookie-issuer` and `oidc-jwks`; owner/S12/S19 still need to choose and configure the production selector. |
| `UAIS_TEACHER_AUTH_SESSION_SIGNING_SECRET` | Missing | Signed teacher-auth session cookies | Blocks trusted teacher-auth issuer and signed teacher workflow route smoke. |
| `UAIS_TEACHER_AUTH_ISSUER_SECRET` | Missing | Signed trusted issuer proof for teacher-auth cookie issuance | Blocks production trusted teacher-auth cookie issuance and protected route smoke. |
| `UAIS_TEACHER_AUTH_OIDC_ISSUER` | Missing | OIDC/JWKS teacher identity provider issuer | Required when `UAIS_TEACHER_AUTH_PROVIDER=oidc-jwks`; blocks production OIDC teacher-auth issuance until configured as a remote HTTPS endpoint. |
| `UAIS_TEACHER_AUTH_OIDC_AUDIENCE` | Missing | OIDC/JWKS teacher identity provider audience | Required when `UAIS_TEACHER_AUTH_PROVIDER=oidc-jwks`; blocks production OIDC teacher-auth issuance until configured. |
| `UAIS_TEACHER_AUTH_OIDC_JWKS_URL` | Missing | OIDC/JWKS public signing-key endpoint | Required when `UAIS_TEACHER_AUTH_PROVIDER=oidc-jwks`; blocks production OIDC token verification until configured as a remote HTTPS endpoint. |
| `UAIS_TEACHER_AUTH_OIDC_TEACHER_ID_CLAIM` | Missing | OIDC/JWKS teacher identity claim selector | Required when `UAIS_TEACHER_AUTH_PROVIDER=oidc-jwks`; blocks production teacher-id derivation until configured. |
| `UAIS_TEACHER_AUTH_OIDC_SMOKE_BEARER_TOKEN` | Missing | Approved short-lived OIDC teacher smoke token | Required only for approved live route smoke when `UAIS_TEACHER_AUTH_PROVIDER=oidc-jwks`; never sync to Vercel or store in reports. |
| `UAIS_TEACHER_AUTH_OIDC_SMOKE_TEACHER_ID` | Missing | Redacted OIDC teacher smoke fixture id | Required only for approved live route smoke when `UAIS_TEACHER_AUTH_PROVIDER=oidc-jwks`; should name the teacher identity represented by the smoke token. |
| `UAIS_TEACHER_AI_OWNERSHIP_BACKEND` | Missing | Durable teacher ownership backend selector | Blocks production durable teacher ownership readiness. |
| `UAIS_VOICE_LIFECYCLE_AUDIT_BACKEND` | Missing | Durable Qwen lifecycle audit backend selector | Blocks production voice lifecycle governance readiness. |
| `UAIS_EXTERNAL_STORAGE_BASE_URL` | Missing | External durable storage endpoint | Blocks external durable storage adapter and smoke; Vercel env apply evidence now also requires this endpoint to classify as remote HTTPS before release-gate env placement can satisfy. |
| `UAIS_EXTERNAL_STORAGE_ACCESS_TOKEN` | Missing | External durable storage auth | Blocks external durable storage adapter and smoke. |
| `UAIS_DEPLOYMENT_BASE_URL` | Missing | Deployed UAIS URL for smoke | Blocks deployed `/teaching` smoke and protected route smoke. |
| `UAIS_EXTERNAL_STORAGE_SMOKE_TEACHER_ID` or `--teacher-id` | Missing | External storage smoke fixture teacher id | Blocks external storage live smoke even after URL/token exist. |

## Recommended Release Sequence

0. S22 use the redacted production E2E orchestration plan as the single release-proof checklist.
   - New dry-run command:
     - `node scripts/production-e2e-orchestrator.mjs --dry-run --report-date 2026-06-17 --env-file .env.local`
   - Current machine-readable plan:
     - `coordination/reports/2026-06-17-production-e2e-orchestration-plan.json`
   - The plan orders the required proof chain: Vercel project readiness, owner-approved Vercel env apply evidence, owner-approved teacher auth provider readiness evidence, redacted external storage production launch contract, redacted external storage container build-readiness evidence, owner-approved external storage service readiness evidence, owner-approved Vercel production deployment evidence, deployed `/teaching` HTML smoke, deployed `/teaching` browser interaction smoke, deployed `/learning` Kang Xia PPT playback smoke, protected deployment route smoke, production external storage smoke, manual PowerPoint/WPS acceptance, then the aggregate production E2E release gate.
   - The plan is intentionally non-mutating in dry-run mode. It marks remote mutation steps such as Vercel env apply, Vercel production deployment, and production external storage smoke with `mutatesRemote: true` and `requiresOwnerApproval: true`.
   - The Vercel production deployment step now uses `scripts/vercel-production-deployment-evidence.mjs`, not a raw `vercel deploy --token` command. It requires ready Vercel project-readiness evidence and applied Vercel env-sync evidence with redacted external-storage fingerprint proof plus redacted apply-preflight and apply-summary proof before any live deploy, `VERCEL_TOKEN` must be supplied through the execution environment or owner-approved env source, and the harness omits deployment URL(s), token, CLI stdout/stderr, Vercel identifiers, team scope values, evidence paths, and local paths from evidence.
   - The deployed `/teaching` smoke step now explicitly records the rendered UI proof markers it must prove: voice sample select, uploaded audio payload, selected sample identity, signed-session bootstrap, protected workflow action manifest, server workflow status, deployment fingerprint, rendered-page fingerprint, and remote-HTTPS deployment origin. This keeps the orchestration checklist aligned with the hardened deployed page smoke and aggregate release gate.
   - The deployed browser smoke step records the non-mutating browser interaction proof it must show: real browser hydration, fixture-intercepted workflow APIs, voice sample file selection, signed-session request, protected workflow click sequence, per-slide WAV download links, no remote provider/storage mutation, and matching deployment fingerprint bound to Vercel production deployment evidence.
   - The Vercel env apply, teacher-auth provider readiness, external-storage service readiness, production deployment, deployed page smoke, browser smoke, protected route smoke, production external storage smoke, and manual PPT acceptance commands now all include `--release-run-id <release-run-id>`. Use the same non-secret run id for one approved release attempt; the final aggregate gate redacts the value and compares internal equality only.
   - The plan now includes redacted `evidenceReadiness` for each expected upstream evidence file, so S22/S19/S24 can see whether the release-proof chain has present, missing, blocked, or invalid evidence before re-running the final gate.
   - Current readiness snapshot: Vercel project readiness evidence is present but blocked; the external storage production launch contract is present and ready as local/redacted launch evidence; the external storage container build-readiness evidence is present but blocked because Docker client is present while the daemon is unavailable; the aggregate release gate evidence is present but blocked; owner-approved Vercel env apply, owner-approved teacher auth provider readiness, owner-approved external storage service readiness, owner-approved Vercel production deployment, deployed teacher workflow page smoke, deployed browser interaction smoke, protected deployment route smoke, production external storage smoke, and manual PPT/WPS acceptance evidence are still missing.
   - `evidenceReadiness` includes only evidence file names, target labels, status labels, and blocked-reason identifiers. It omits local paths, env-file paths, deployment URLs, response bodies, and credential values.
   - The plan omits env-file paths, deployment URLs, response bodies, and credential values. It does not replace the actual live evidence files required by the aggregate gate.

1. S12/S19 choose and document the production teacher-auth/session provider boundary.
   - Current implemented provider selectors for `UAIS_TEACHER_AUTH_PROVIDER`: `trusted-cookie-issuer` and `oidc-jwks`.
   - Required server-only secret for both selectors: `UAIS_TEACHER_AUTH_SESSION_SIGNING_SECRET` for signed cookies.
   - Additional `trusted-cookie-issuer` secret: `UAIS_TEACHER_AUTH_ISSUER_SECRET` for signed trusted issuer proof.
   - Additional `oidc-jwks` config: `UAIS_TEACHER_AUTH_OIDC_ISSUER`, `UAIS_TEACHER_AUTH_OIDC_AUDIENCE`, `UAIS_TEACHER_AUTH_OIDC_JWKS_URL`, and `UAIS_TEACHER_AUTH_OIDC_TEACHER_ID_CLAIM`.
   - `scripts/teacher-auth-provider-readiness.mjs` is now the non-mutating production provider-readiness gate before protected route smoke:
     - `node scripts/teacher-auth-provider-readiness.mjs --live --approved --environment production --env-file <env-file> --release-run-id <release-run-id> --vercel-env-sync <vercel-env-sync-evidence> > <evidence>`
   - The readiness evidence must prove `target: "teacher-auth-provider-readiness"`, live production `status: "ready"`, an accepted provider selector, matching Vercel env-sync provider-selector guard, matched same-run Vercel env-sync release proof, sufficient teacher-auth session signing secret, hardened cookie contract, exact two-cookie production contract for `uais_teacher_auth_claims` and `uais_teacher_auth_signature`, no-cookie-issued safety, provider-specific readiness, and the release-run id for the approved production attempt. For `trusted-cookie-issuer`, the trusted issuer secret must be sufficient and the issuer proof must bound cookie Max-Age. For `oidc-jwks`, issuer and JWKS endpoints must classify as remote HTTPS and audience plus teacher-id claim must be present. Provider URLs, secrets, response bodies, local paths, and cookie values remain omitted.
   - `scripts/teacher-auth-provider-readiness.mjs --live --approved` now blocks outside `--environment production` with `teacher-auth-provider-readiness-not-production`, preventing preview/local checks from producing reusable `status: "ready"` evidence. The aggregate gate still independently blocks stale non-production ready JSON with the same reason.
   - The aggregate gate now blocks with `teacher-auth-provider-readiness-not-live-ready` while only dry-run evidence is present, and rejects stale live-ready readiness JSON with `teacher-auth-session-cookie-pair-contract-not-proven` if the exact production cookie pair contract is missing. This is separate from the later protected route smoke that proves issued cookies work against deployed routes.
   - Once Vercel env-sync, teacher-auth provider readiness, and protected route smoke all have production evidence, the aggregate gate compares their provider selectors and blocks mixed selector chains with `teacher-auth-provider-selector-mismatch`.
   - The runtime teacher-auth provider contract now emits redacted `secretStrength` checks with a 32-character minimum and blocks production readiness when the teacher-auth session signing secret, or the trusted issuer secret for `trusted-cookie-issuer`, is weak even if it is present. It also emits redacted `trustedIssuerSeparation` for `trusted-cookie-issuer` and blocks production readiness when the teacher-auth session signing value and trusted issuer value are the same.
   - Trusted teacher-auth cookies are now required to be the two named cookies `uais_teacher_auth_claims` and `uais_teacher_auth_signature`, with `HttpOnly`, `SameSite=Lax`, `Secure` in production, `Path=/`, bounded by `Max-Age` no longer than the signed trusted issuer proof TTL, and `Priority=High`.
   - `/api/ai/teacher-auth/issue` now requires both signed admin AI access and matching signed trusted issuer proof for the requested teacher id before it can issue teacher-auth cookies.
   - `/api/ai/teacher-auth/issue` can now also verify an `oidc-jwks` bearer token against the configured issuer, audience, JWKS key, token lifetime, and teacher-id claim before issuing the same hardened teacher-auth cookie pair. The response omits bearer tokens, JWKS content, provider URLs, and session ids.
   - OIDC/JWKS cookie lifetime is capped to the verified token expiry, matching the existing trusted-issuer TTL-bounding pattern.
   - The local S05 website workflow now calls `/api/ai/session` before protected voice/PPT workflow calls and forwards the returned signed AI access headers when present.
   - Current route smoke contract now chains `/api/ai/teacher-auth/issue` Set-Cookie headers into `/api/ai/session` and then `/api/ai/teacher-ppt-workflow`; local self-signed teacher cookies no longer satisfy the protected route smoke harness.
   - The route smoke harness now supports `UAIS_TEACHER_AUTH_PROVIDER=oidc-jwks` by sending a redacted `UAIS_TEACHER_AUTH_OIDC_SMOKE_BEARER_TOKEN` to `/api/ai/teacher-auth/issue`, using `UAIS_TEACHER_AUTH_OIDC_SMOKE_TEACHER_ID` as the smoke resource owner, and omitting the bearer token, OIDC provider values, and response bodies from output.
   - The route smoke contract now also calls `/api/ai/teacher-ownership` with the issuer-provided teacher cookie. That route now uses the same teacher ownership adapter as `/api/ai/session`, so `UAIS_TEACHER_AI_OWNERSHIP_BACKEND=external` no longer leaves the ownership summary route stranded on `UAIS_TEACHER_AI_OWNERSHIP_DIR`.
   - `/api/ai/session` successful responses now include the redacted `authProviderContract`; route smoke requires `authProviderContract: present` in the teacher AI session response shape, and the local production E2E diagnostic preserves that proof without response bodies.
   - The total production release gate also rejects `live`/`passed` route-smoke evidence when `s22-teacher-ai-session-route` or `s22-teacher-ownership-route` is missing, when either the teacher ownership summary route or teacher workflow route does not report `auth: "issued-teacher-auth-cookie"`, or when hardened issuer cookie headers are not proven.
   - The aggregate production gate now rejects trusted-issuer route-smoke evidence with `authProviderMode: "trusted-cookie-issuer"` unless `s22-teacher-auth-issuer-route` reports `auth: "signed-admin-ai-access"`, preventing stale or provider-mismatched issuer evidence from satisfying the trusted path.
   - The aggregate production gate now also rejects route-smoke evidence with `authProviderMode: "oidc-jwks"` unless `s22-teacher-auth-issuer-route` reports `auth: "oidc-jwks-bearer-token"`, preventing trusted-issuer smoke evidence from satisfying an OIDC production proof.
   - The aggregate production gate now also rejects otherwise complete `live`/`passed` production route-smoke evidence unless `authProviderMode` explicitly proves an accepted selector: `trusted-cookie-issuer` or `oidc-jwks`.

2. S12/S24/S19 provision the real external durable storage service.
   - Local runnable reference service now exists:
     - `node scripts/external-storage-service.mjs --data-dir <durable-data-dir>`
     - Server token source: `UAIS_EXTERNAL_STORAGE_ACCESS_TOKEN`
     - Local reference evidence: `coordination/reports/2026-06-17-external-storage-reference-service-live-smoke.json`
   - This reference service passed a localhost live smoke, but its evidence is explicitly `local-reference` and does not satisfy the production release gate.
   - The localhost live smoke now performs a redacted health backing-store `write-read-delete` probe, redacted write/read verification for teacher ownership merge and Qwen lifecycle audit append, not just pre-seeded reads. It also generates per-run smoke marker IDs and requires the subsequent ownership and lifecycle-audit reads to prove `runScopedSmokeGrant` and `runScopedSmokeAuditEvent`; marker values stay omitted from output.
   - `scripts/external-storage-smoke.mjs` now emits only redacted endpoint classification and a redacted service-origin fingerprint instead of the storage URL: `networkClass` distinguishes remote/local/private/missing/invalid hosts, `endpointClass` distinguishes production-eligible `remote-https` from `insecure-http`, `local-loopback`, `private-network`, `missing`, or `invalid`, and `storageServiceFingerprint` records only `present`/`missing`/`invalid` status plus a short SHA-256 token when an origin exists. The local reference service evidence reports `local-loopback` for both endpoint fields.
   - `scripts/external-storage-service.mjs` now supports `--service-mode production`, which changes startup and `/healthz` identity to `target: "uais-external-storage-production-service"` and refuses production mode unless the access token is strong. This is a deployable service identity guard only; production release readiness still requires a real remote-HTTPS service and durable backing store evidence.
   - `scripts/external-storage-service-production-launcher.mjs --dry-run` now emits `coordination/reports/2026-06-17-external-storage-production-launch-contract.json`, a redacted launch contract requiring env-only access-token delivery, `UAIS_EXTERNAL_STORAGE_DATA_DIR` as a persistent data directory, `PORT` as the platform port source, forced `--service-mode production`, and a container runtime artifact made of `Dockerfile.external-storage` plus `.dockerignore`. It omits the token value, data directory value, and local paths.
   - `scripts/external-storage-container-build-readiness.mjs --dry-run` now emits `coordination/reports/2026-06-17-external-storage-container-build-readiness.json`, proving the Dockerfile contract and Docker context secret/generated-output exclusion while recording redacted Docker client/daemon availability. The current local evidence is blocked because the Docker client is present but the daemon is unavailable, so no image build has been proved yet.
   - `scripts/external-storage-service-readiness.mjs` is now the non-mutating production service candidate gate before write/read smoke:
     - `node scripts/external-storage-service-readiness.mjs --live --approved --environment production --env-file <env-file> --release-run-id <release-run-id> --vercel-env-sync <vercel-env-sync-evidence> > <evidence>`
   - The readiness evidence must prove `target: "external-storage-service-readiness"`, live production `status: "ready"`, remote-HTTPS storage endpoint classification, redacted service-origin fingerprint, matching Vercel env-sync storage fingerprint guard, matched same-run Vercel env-sync release proof, `/healthz` production service identity with `target: "uais-external-storage-production-service"`, API contract version, durable backing-store readiness, redacted health proof, no write operations, omission of service URL, response bodies, local paths, and values, and the release-run id for the approved production attempt.
   - The aggregate gate now blocks with `external-storage-service-readiness-not-live-ready` while only dry-run evidence is present, blocks otherwise live-ready production readiness evidence with `external-storage-service-readiness-fingerprint-not-proven` if the redacted service-origin fingerprint is missing, and blocks matched-but-stale env-sync binding summaries with `external-storage-service-vercel-env-sync-release-run-not-proven` when `vercelEnvSyncEvidence.releaseRunIdStatus` is not `matched`. This is separate from the later `external-storage-smoke` write/read proof. Once production storage evidence exists, `external-storage-service-consistency` compares the redacted readiness and smoke fingerprint statuses; when applied Vercel env-sync evidence is also present, it must carry a matching redacted `externalStorageServiceFingerprint` for the deployed `UAIS_EXTERNAL_STORAGE_BASE_URL`.
   - The runtime external storage backend contract now emits redacted `secretStrength` checks with a 32-character minimum and blocks production readiness for both teacher ownership and voice lifecycle audit adapters when `UAIS_EXTERNAL_STORAGE_ACCESS_TOKEN` is weak even if the storage URL and token are present.
   - The local production E2E diagnostic now starts this reference service before `next start` and configures both `UAIS_TEACHER_AI_OWNERSHIP_BACKEND=external` and `UAIS_VOICE_LIFECYCLE_AUDIT_BACKEND=external` through ephemeral fixture env. This proves the production-mode app can read teacher ownership and lifecycle audit readiness through the external adapter locally, but it remains non-release evidence until the same harnesses pass against the real deployed storage service.
   - Required service shapes:
     - `GET /healthz`
     - `GET /teacher-ai-ownership/{teacherId}`
     - `POST /teacher-ai-ownership/{teacherId}/merge`
     - `GET /qwen-voice-lifecycle-audit`
     - `POST /qwen-voice-lifecycle-audit`
   - Required server-only env:
     - `UAIS_TEACHER_AI_OWNERSHIP_BACKEND` with selector `external`
     - `UAIS_VOICE_LIFECYCLE_AUDIT_BACKEND` with selector `external`
     - `UAIS_EXTERNAL_STORAGE_BASE_URL`
     - `UAIS_EXTERNAL_STORAGE_ACCESS_TOKEN`

3. S19 place server-only variables in the linked Vercel project after owner approval.
   - Current link readiness: `coordination/reports/2026-06-17-vercel-link-deploy-readiness.md`.
   - Machine-readable link evidence: latest fresh evidence is `coordination/reports/2026-06-19-vercel-project-readiness.json`; older 2026-06-17/2026-06-18 files remain historical snapshots.
   - Vercel CLI is now available as a project-local dev dependency (`vercel` `^54.14.0`); `scripts/vercel-project-readiness.mjs` and `scripts/vercel-env-sync.mjs --apply` can use `node_modules/.bin/vercel` without relying on a global install.
   - The link-readiness evidence now requires redacted Vercel auth presence, exactly one current team scope, a deterministic exact project candidate, and explicit safety proof that values, account names, team IDs/slugs, project/org IDs, project names, deployment URLs, and local private paths are omitted before the release gate can treat project linking as ready.
   - Current candidate status from 2026-06-19 evidence: Vercel CLI/auth are present, but current team scope is missing (`teamCount: 0`), both `uais` and `uais-top` checks have 0 filtered and 0 exact project matches, and the link marker is missing. The next owner decision is first to select or confirm the intended Vercel team scope, then create/rename/select an approved Vercel project name and pass it with `--project-name <approved-name>`, or provide the approved Vercel project id via `--project-id <approved-project-id>`. The project id path records only `approved-redacted-project-id-provided`; the id value remains omitted.
   - `npm audit --omit=dev` reports 0 production dependency vulnerabilities after adding the dev CLI. The Vercel CLI dependency tree still reports dev-tooling audit findings through normal `npm install` output; do not run broad `npm audit fix --force` without S10/S22 review.
   - Link command before apply: `npx vercel link`.
   - Candidate/link readiness command after choosing the project name:
     - `node scripts/vercel-project-readiness.mjs --project-name <approved-name>`
   - Candidate/link readiness command after choosing the project id:
     - `node scripts/vercel-project-readiness.mjs --project-id <approved-project-id>`
   - Dry-run command:
     - `node scripts/vercel-env-sync.mjs --dry-run --project uais --env-file .env.local --release-run-id <release-run-id>`
   - Apply command after approval and linked project confirmation:
     - `node scripts/vercel-env-sync.mjs --apply --approved --project uais --env-file .env.local --vercel-project-readiness coordination/reports/2026-06-17-vercel-project-readiness.json --release-run-id <release-run-id> > <evidence>`
   - Apply mode refuses to call `vercel env add` unless the supplied project-readiness evidence is `ready` and contains present CLI/auth/team/project-candidate/project-link/upload-ignore checks.
   - `scripts/vercel-env-sync.mjs` now emits redacted `localOnlyEntries` for `UAIS_TEACHER_AUTH_OIDC_SMOKE_BEARER_TOKEN` and `UAIS_TEACHER_AUTH_OIDC_SMOKE_TEACHER_ID` with `deploymentAction: "ignored"`; these smoke-only variables stay out of deployable env entries.
   - The aggregate release gate rejects stale apply evidence unless the env-sync output includes `target: "vercel-env-sync"`, `projectReadinessEvidenceStatus: "ready"`, explicit `authProviderMode` proof, target coverage for both `production` and `preview`, and safety proof `localOnlySmokeEnvNotSynced: true`.
   - Vercel env evidence is now selector-aware: `trusted-cookie-issuer` requires `UAIS_TEACHER_AUTH_ISSUER_SECRET`; `oidc-jwks` requires `UAIS_TEACHER_AUTH_OIDC_ISSUER`, `UAIS_TEACHER_AUTH_OIDC_AUDIENCE`, `UAIS_TEACHER_AUTH_OIDC_JWKS_URL`, and `UAIS_TEACHER_AUTH_OIDC_TEACHER_ID_CLAIM`, without requiring the trusted issuer secret.
   - `scripts/vercel-env-sync.mjs` now emits only redacted OIDC endpoint classes under `oidcEndpointSecurity` when `authProviderMode` is `oidc-jwks`; the aggregate release gate rejects OIDC env apply evidence unless both issuer and JWKS classify as `remote-https`.
   - `scripts/vercel-env-sync.mjs` now also emits a redacted `externalStorageEndpoint` class and `externalStorageServiceFingerprint` for `UAIS_EXTERNAL_STORAGE_BASE_URL`; the aggregate release gate rejects env apply evidence unless the class is `remote-https` and the fingerprint proof is present, and later rejects mixed evidence unless the Vercel env fingerprint matches storage readiness and smoke.
   - `scripts/vercel-env-sync.mjs` now emits redacted `secretStrength` checks with a 32-character minimum for production-critical signing/token secrets; the aggregate release gate rejects apply evidence unless every required check is `sufficient`.
   - `scripts/vercel-env-sync.mjs --apply` now emits a redacted `applySummary` with applied entry/action counts, production/preview action counts, local-only skipped-entry count, `valuesRedacted: true`, and `cliOutputOmitted: true`. The aggregate release gate rejects otherwise complete apply JSON with `vercel-env-apply-summary-not-proven` when this summary is missing or incomplete.

4. S22 create or inspect the owner-approved Vercel production deployment, then run deployed smoke after the deployment URL exists.
   - New orchestration evidence slot:
     - `coordination/reports/2026-06-17-vercel-production-deployment.json`
   - The aggregate release gate now blocks with `vercel-production-deployment-not-proven` until this evidence exists and proves `target: "vercel-production-deployment"`, live production deployed status, a redacted deployment fingerprint, remote-HTTPS deployment origin, ready project-readiness guard, applied env-sync guard, explicit env-sync apply-preflight guard, deployment observation, and redaction safety.
   - The redacted deployment fingerprint in this evidence is the production fingerprint the deployed page smoke must match; the aggregate gate rejects mixed-deployment page evidence with `vercel-production-deployment-fingerprint-mismatch`.
   - The dry-run orchestration plan records the remote mutation command with placeholders only:
     - `node scripts/vercel-production-deployment-evidence.mjs --live --approved --deploy --environment production --env-file <env-file> --scope <approved-scope> --vercel-project-readiness <evidence> --vercel-env-sync <evidence> --release-run-id <release-run-id> > <evidence>`
   - This step must not run until the owner has approved the Vercel project/scope/token path and S19 has applied the required server-only env with redacted storage-origin fingerprint proof plus redacted apply-preflight and apply-summary proof. The harness refuses `--token`; `VERCEL_TOKEN` must be available in the process environment or the approved env file used for the run. The evidence redacts the deployment URL and records only a deployment fingerprint plus remote-HTTPS origin class for the subsequent page/browser/route smokes.

5. S22 run deployed smoke after Vercel deployment URL exists.
   - Local production diagnostic already passed:
     - `node scripts/local-production-e2e-smoke.mjs --live --approved --timeout-ms 240000`
     - Evidence: `coordination/reports/2026-06-18-local-production-e2e-smoke.json`
     - This proves local build/start, local external storage reference service startup, external teacher ownership/lifecycle backend wiring, `/learning` Kang Xia 19-slide playback manifest plus first WAV headers, `/teaching` rendered markers, real browser interaction through the teacher workflow with short-upload duration gating and 19 WAV links, signed teacher ownership summary reads, and protected route behavior only; it is not production release-gate eligible.
   - Deployed teacher page:
    - `node scripts/teacher-workflow-deployment-smoke.mjs --live --approved --environment production --base-url <deployment-url> --env-file .env.local --release-run-id <release-run-id> --vercel-production-deployment <vercel-production-deployment-evidence>`
     - The aggregate gate rejects deployed page evidence unless `target` is `teacher-workflow-deployment-smoke`.
     - The aggregate production gate rejects local or unspecified page-smoke evidence even when rendered workflow anchors are present.
     - The deployed page smoke must show the voice sample select marker, uploaded audio payload marker, browser metadata duration-gate marker, selected sample identity marker, signed-session bootstrap marker, protected workflow action manifest, and server workflow status marker in the rendered `/teaching` HTML so the production page cannot pass with an older non-session, pre-duration-gate, or pre-server-status workflow bundle. It now also emits a short rendered-page SHA-256 fingerprint of the fetched HTML while omitting the response body. Local component evidence now also proves fail-closed handling when signed AI session bootstrap is denied; production still needs deployed route smoke to prove the server-side auth provider.
     - The deployed page smoke emits a redacted deployment fingerprint, rendered-page fingerprint, and redacted deployment origin class derived from the target origin; the URL value and response body remain omitted.
     - Once Vercel production deployment evidence is present and deployed, the aggregate gate compares the page-smoke deployment fingerprint to that production deployment fingerprint before accepting the page evidence, preventing a page smoke from a different deployment from satisfying the release chain.
   - Deployed teacher browser interaction:
     - `node scripts/teacher-workflow-browser-smoke.mjs --live --approved --environment production --base-url <deployment-url> --env-file .env.local --release-run-id <release-run-id> --vercel-production-deployment <vercel-production-deployment-evidence>`
     - The live browser smoke requires Playwright at execution time and does not run in dry-run mode. Current project dependency status is still empty (`npm ls playwright --depth=0` reports empty), so the production run must use either an approved project-local Playwright install or the documented transient runtime command before browser interaction can proceed.
     - The browser smoke fixture-intercepts `/api/ai/session`, `/api/ai/voice-sample`, `/api/ai/voice-clone/preflight`, `/api/ai/voice-clone/status`, `/api/ai/ppt-narration`, and `/api/ai/teacher-ppt-workflow`, so it proves deployed frontend hydration and interaction wiring without mutating real provider, teacher ownership, lifecycle-audit, or storage services.
     - The aggregate gate only requires this browser smoke after the deployed page HTML smoke is production/live/passed. It then requires matching deployment fingerprint, remote-HTTPS origin, fixture-only API policy, and all browser interaction results `passed`, including the short-upload `voiceSampleDurationGate` proof before the 10-second upload path proceeds.
   - Deployed student PPT playback:
     - `node scripts/learning-ppt-playback-deployment-smoke.mjs --live --approved --environment production --base-url <deployment-url> --env-file .env.local --release-run-id <release-run-id> --vercel-production-deployment <vercel-production-deployment-evidence>`
    - The smoke fetches deployed `/learning`, then the student-safe `/api/learning/ppt-playback/elementary-math-research` manifest, then the first per-slide WAV through `/api/learning/ppt-playback/audio/...`; it must prove Kang Xia identity, `康霞博士克隆声音`, 19 slides, redacted manifest safety, WAV `RIFF/WAVE` headers with at least 1024 bytes of first-slide audio content-length evidence, remote-HTTPS deployment origin, a deployment fingerprint matching the Vercel production deployment evidence, and the smoke harness' redacted `vercelProductionDeploymentEvidence.status: "matched"` summary.
     - The aggregate gate waits for production deployment before requiring this evidence; once deployment is proved, missing or non-live student playback evidence blocks approval with `deployed-learning-ppt-playback-not-live-passed`.
   - Protected routes:
     - `node scripts/ai-route-smoke.mjs --live --approved --environment production --base-url <deployment-url> --env-file .env.local --release-run-id <release-run-id> --vercel-production-deployment <vercel-production-deployment-evidence> --teacher-auth-provider-readiness <teacher-auth-provider-readiness-evidence>`
     - The aggregate gate rejects route evidence unless `target` is `deployment-route-smoke`.
     - The route-smoke harness now blocks production live runs before protected API requests unless the supplied `teacher-auth-provider-readiness` evidence is live production `ready`, uses the same release-run id, and matches the selected `authProviderMode`.
     - The aggregate gate also rejects otherwise passing route-smoke JSON unless it carries the route harness' redacted `teacherAuthProviderReadinessEvidence.status: "matched"` and `teacherAuthProviderReadinessEvidence.releaseRunIdStatus: "matched"` summary, preventing legacy route-smoke evidence from bypassing the teacher-auth readiness binding or reusing readiness from a different release attempt. The same route-smoke JSON must also carry redacted `vercelProductionDeploymentEvidence.status: "matched"`, `deploymentObservationStatus: "observed"`, and `releaseRunIdStatus: "matched"`; stale same-shape deployment bindings now block with `deployment-route-smoke-vercel-deployment-release-run-not-proven`.
     - The route-smoke harness now supports both `trusted-cookie-issuer` and `oidc-jwks`. For `oidc-jwks`, the live run must supply a short-lived `UAIS_TEACHER_AUTH_OIDC_SMOKE_BEARER_TOKEN` and matching `UAIS_TEACHER_AUTH_OIDC_SMOKE_TEACHER_ID`; the token is used only in the request header and is not printed.
     - The aggregate gate rejects production `live`/`passed` route evidence when `authProviderMode` is missing or outside the accepted selectors, even if the route checks, cookie hardening, and response-shape proofs are otherwise complete.
     - For `trusted-cookie-issuer`, the issuer route must report `auth: "signed-admin-ai-access"`; for `oidc-jwks`, it must report `auth: "oidc-jwks-bearer-token"`.
    - The route smoke must show `s22-teacher-auth-issuer-route`, `s22-teacher-ai-session-route`, `s22-teacher-ownership-route`, and `s22-teacher-ppt-workflow-route` all `ok`, with the session, ownership summary, and workflow requests authenticated by the issued teacher cookie pair. The aggregate gate now records `issuerCookieHardening`, `ownershipRouteAuth`, `workflowRouteAuth`, and `routeResponseShapes`; it blocks unless `issuerCookieHardening` is `proved`, both route auth fields are `issued-teacher-auth-cookie`, and all trusted teacher route response-shape entries are `proved`, including the issuer route's redacted `teacherAuthSession`, `authProviderContract`, and S12 issuer boundary proof.
     - If route smoke reports `authProviderMode: "oidc-jwks"`, the aggregate gate also requires the issuer route auth field to be `oidc-jwks-bearer-token`.
     - The issuer route smoke must prove both teacher-auth cookies include the hardened header evidence `teacherAuthClaimsSetCookie`, `teacherAuthSignatureSetCookie`, `httpOnlySameSiteSecureMaxAge`, `priorityHigh`, and `issuerProofBoundedMaxAge`; the aggregate gate reports this as `issuerCookieHardening: "proved"`.
    - The issuer route response-shape proof must show `teacherAuthSession`, redacted `authProviderContract`, and the S12 trusted teacher-auth issuer boundary; the session route response-shape proof must show `accessSession`, `accessPlan`, and the S12 teacher AI session boundary; the ownership route proof must show `ownership`, `consistency`, and the S12 teacher ownership auth boundary; the PPT workflow route proof must show the workflow object plus the OpenMAIC-style agent handoff plan and S22 release-smoke handoff.
     - The aggregate production gate rejects local or unspecified route-smoke evidence even when all protected route checks are `ok`.
     - The aggregate production gate must report `issuerCookieHardening: "proved"`, `ownershipRouteAuth: "issued-teacher-auth-cookie"`, `workflowRouteAuth: "issued-teacher-auth-cookie"`, and `routeResponseShapes` values of `proved` for `deployment-route-smoke`.
     - The aggregate production gate now rejects production page smoke without redacted deployment fingerprint evidence, rendered-page fingerprint evidence, or remote-HTTPS deployment origin proof, and also rejects page/route smoke pairs whose deployment fingerprints are missing or do not match, preventing mixed-deployment evidence from satisfying the chain.
     - The aggregate production gate now rejects otherwise complete protected route smoke unless `deploymentOrigin.originClass` is `remote-https`, preventing localhost, private-network, insecure HTTP, invalid, or missing route-smoke origins from satisfying production.
   - External storage:
     - `node scripts/external-storage-service-readiness.mjs --live --approved --environment production --env-file .env.local --release-run-id <release-run-id> --vercel-env-sync <vercel-env-sync-evidence>`
     - The aggregate gate rejects the production chain until this service readiness evidence proves a remote-HTTPS endpoint, redacted service-origin fingerprint, `/healthz` production service identity with `target: "uais-external-storage-production-service"`, API contract version, durable backing-store readiness, redacted health proof, no-write readiness semantics, the approved release-run id, redacted `vercelEnvSyncEvidence.releaseRunIdStatus: "matched"`, and consistency with the redacted Vercel env storage fingerprint once env apply evidence exists.
     - `node scripts/external-storage-smoke.mjs --live --approved --environment production --teacher-id <redacted-smoke-teacher-id> --env-file .env.local --release-run-id <release-run-id> --external-storage-service-readiness <external-storage-service-readiness-evidence>`
     - The aggregate gate rejects production storage smoke evidence unless it comes from the `external-storage-smoke` harness and carries redacted `externalStorageServiceReadinessEvidence.releaseRunIdStatus: "matched"` from the same service-readiness evidence.
     - The aggregate gate also rejects a production readiness/smoke pair whose redacted storage-service fingerprints are missing or do not match, preventing readiness for one external service from being combined with write/read smoke from another service.
     - The smoke must pass `s22-external-storage-health`, `s12-external-teacher-ownership-merge`, `s12-external-teacher-ownership-read`, `s24-external-lifecycle-audit-append`, and `s24-external-lifecycle-audit-read`.
     - The health response-shape check must confirm `durableBackingStore: "present"` from a redacted backing-store `write-read-delete` readiness probe; a process-only health response no longer satisfies production storage evidence.
     - The aggregate gate now records `storageResponseShapes` and blocks production storage evidence unless the health, ownership merge, ownership read, lifecycle audit append, and lifecycle audit read response-shape entries are all `proved`.
     - The read response-shape checks must confirm the same-run smoke ownership grant and lifecycle audit marker created earlier in the same approved run via `runScopedSmokeGrant` and `runScopedSmokeAuditEvent`; stale fixed marker evidence no longer satisfies the production gate.
     - The aggregate gate now also rejects production storage evidence unless the redacted storage endpoint classification is both `networkClass: "remote"` and `endpointClass: "remote-https"`, preventing localhost, private-network, insecure HTTP, invalid, or missing storage smoke evidence from satisfying the production requirement when mislabeled as `production`.

6. S24 record manual PowerPoint/WPS playback acceptance.
   - Use `coordination/reports/2026-06-17-kangxia-ppt-manual-playback-acceptance-checklist.md`.
   - Readiness evidence: `coordination/reports/2026-06-17-kangxia-ppt-manual-playback-readiness.md`.
   - Fillable record template: `coordination/reports/2026-06-17-kangxia-ppt-manual-acceptance-record-template.json`.
   - The package JSON and template record now carry the same final narrated PPTX artifact fingerprint; the gate reports `packageArtifactFingerprintStatus: "present"` and `manualRecordArtifactFingerprintStatus: "matched"` while still blocking because no human playback has been recorded.
   - Feed the redacted manual record into `scripts/ppt-manual-playback-acceptance.mjs`.
   - The manual record must include a package identity that matches the current narrated PPT package, the same non-secret `releaseRunId` used by the production deployment and smoke evidence, the expected redacted production deployment fingerprint, non-empty tester evidence, a valid non-future tested-at timestamp, non-empty PowerPoint and WPS version evidence, and every prefilled slide/audio id marked with both `audioPlays: true` and `heardTargetVoice: true` in both target applications.
   - The package JSON and completed manual record must keep the same final narrated PPTX artifact fingerprint. The acceptance gate compares the value internally but emits only `packageArtifactFingerprintStatus` and `manualRecordArtifactFingerprintStatus`, not the fingerprint value.
   - `scripts/ppt-manual-playback-acceptance.mjs` now blocks wrong-package manual records with `manual-record-package-mismatch` and complete-but-unbound records with `manual-record-package-identity-missing`.
   - `scripts/ppt-manual-playback-acceptance.mjs` now blocks complete records from a different PPT artifact with `manual-record-artifact-fingerprint-mismatch`; the aggregate production gate blocks missing/mismatched artifact proof with `manual-ppt-artifact-fingerprint-not-proven`.
   - `scripts/ppt-manual-playback-acceptance.mjs` now emits redacted `manualRecordTimingStatus` and blocks future `testedAt` values with `manual-record-tested-at-in-future`; tested-at values themselves stay out of generated evidence.
   - `scripts/ppt-manual-playback-acceptance.mjs` now blocks complete records whose manual `releaseRunId` is missing or different from the command `--release-run-id`, while emitting only `manualRecordReleaseRunStatus`.
   - `scripts/ppt-manual-playback-acceptance.mjs` now also blocks release-run-bound complete records when the deployment binding is missing, or when the manual record omits/mismatches the expected deployment fingerprint; the aggregate gate rejects accepted JSON unless `manualRecordDeploymentFingerprintStatus` is `matched`.
   - Run `scripts/ppt-manual-playback-acceptance.mjs` with `--vercel-production-deployment <evidence>` so it derives the same release-run id, redacted deployment fingerprint, and deployment observed timestamp from the approved Vercel production deployment evidence. The supplied evidence must be `target: "vercel-production-deployment"`, `mode: "live"`, `environment: "production"`, and `status: "deployed"`; preview, dry-run, blocked, failed, or target-mismatched deployment JSON cannot bind final manual acceptance. The lower-level `--release-run-id`, `--deployment-fingerprint`, and `--deployment-observed-at` options remain available for fixture diagnostics, but final S24 evidence should use the Vercel deployment evidence file as the source.
   - The PPT acceptance output now reports `deploymentEvidenceSource: "vercel-production-deployment"` and `deploymentObservationBindingStatus: "proved"` when that authoritative deployment evidence file is used; observed timestamps themselves stay out of the generated acceptance evidence.
   - The aggregate production gate rejects stale accepted JSON unless `target` is `ppt-manual-playback-acceptance`, `mode` is `record`, `manualRecordPackageIdentityStatus` is `matched`, `manualRecordTimingStatus` is `valid-past-or-present`, `manualRecordReleaseRunStatus` is `matched`, `manualRecordDeploymentFingerprintStatus` is `matched`, `manualRecordAfterDeploymentStatus` is `proved`, a top-level `releaseRunId` is present, `machinePreflightStatus` is `passed`, `manualRecordEvidenceStatus` is `complete`, both required applications are accepted, and both `expectedSlideCount` and checklist `slideChecks` prove 19 slide checks whose record rows stayed bound to the expected slide/audio ids and target cloned voice.

7. S22 re-run the total production E2E release gate.
   - The orchestration plan records the expected evidence filenames and the key proof markers for each upstream step and the final aggregate gate.
   - The final orchestration command now passes both `--vercel-production-deployment <evidence>` and `--learning-ppt-playback <evidence>` to `scripts/production-e2e-release-gate.mjs`; omitting production deployment evidence keeps the gate blocked, and omitting deployed student playback evidence blocks after production deployment is proved.
   - The Vercel production deployment command itself now includes `--vercel-project-readiness <evidence>` and `--vercel-env-sync <evidence>`; the aggregate gate requires the resulting deployment evidence to prove `projectReadinessGuard: "proved"`, `envSyncGuard: "proved"`, and `deploymentObservationStatus: "proved"` before it can satisfy the deployment requirement.
   - The final aggregate gate also requires the same release-run id across Vercel env apply, teacher-auth provider readiness, external-storage service readiness, Vercel production deployment, deployed page smoke, deployed browser smoke, deployed student PPT playback smoke, protected route smoke, production external storage smoke, and manual PPT acceptance. Mixed evidence blocks with `production-release-run-id-mismatch`.
   - Use the accepted/live evidence files from steps 3-5.
   - The gate must return `status: "ready"` before the full enterprise UAIS objective can be considered complete.

## Current Non-Completion Statement

Additional deployment guard note: production deployment evidence is not acceptable unless it proves both `projectReadinessGuard: "proved"` and `envSyncGuard: "proved"`; the live deployment harness now requires ready Vercel project-readiness evidence and applied Vercel env-sync evidence from the same `releaseRunId` with redacted storage-origin fingerprint proof before invoking Vercel.

Additional Vercel deployment inspect guard note: `scripts/vercel-production-deployment-evidence.mjs --live --approved --environment production --deployment-url <deployment-url>` now follows the same project/env evidence guard as `--deploy`; it blocks before `vercel inspect` unless ready project-readiness evidence and same-run applied Vercel env-sync evidence are supplied. Inspecting an existing production URL can no longer create reusable deployment evidence without the project link and env apply proof required by the final release gate.

Additional teacher-auth release-run binding guard note: `scripts/teacher-auth-provider-readiness.mjs --live --approved --environment production` now emits redacted `vercelEnvSyncEvidence.releaseRunIdStatus` and the aggregate production gate rejects stale readiness JSON that has matched env-sync target/status/apply-preflight but lacks same-run release proof. Teacher-auth readiness can no longer satisfy final approval with old JSON that is provider-matched but unbound to the approved env-sync release run.

Additional storage deployment guard note: `Dockerfile.external-storage` and `.dockerignore` now define a minimal container artifact for the external storage service, but this artifact is only a packaging prerequisite; production completion still requires a remote-HTTPS deployment with owner-approved env, live readiness, and live write/read smoke.

Additional external-storage readiness guard note: approved non-production `/healthz` diagnostics may still record redacted health details for local/preview troubleshooting, but `scripts/external-storage-service-readiness.mjs --live --approved` now keeps those runs blocked with `external-storage-service-readiness-not-production`; only `--environment production` can issue reusable ready evidence for the release chain.

Additional external-storage release-run binding guard note: `scripts/external-storage-service-readiness.mjs --live --approved --environment production` now emits redacted `vercelEnvSyncEvidence.releaseRunIdStatus` and the aggregate production gate rejects stale readiness JSON that has matched env-sync target/status/apply-preflight but lacks same-run release proof. `scripts/external-storage-smoke.mjs --live --approved --environment production` now emits redacted `externalStorageServiceReadinessEvidence.releaseRunIdStatus`; the aggregate gate rejects stale production storage-smoke JSON without both `externalStorageServiceReadinessEvidence.status: "matched"` and `externalStorageServiceReadinessEvidence.releaseRunIdStatus: "matched"`, so write/read smoke cannot satisfy final approval without prior same-run service readiness.

Additional protected route-smoke release-run binding guard note: `scripts/ai-route-smoke.mjs --live --approved --environment production` now emits redacted `releaseRunIdStatus` summaries for both `vercelProductionDeploymentEvidence` and `teacherAuthProviderReadinessEvidence`. The aggregate production gate rejects otherwise passing route-smoke JSON when the teacher-auth readiness binding is matched by target/provider/status but lacks `teacherAuthProviderReadinessEvidence.releaseRunIdStatus: "matched"` (`deployment-route-smoke-teacher-auth-readiness-release-run-not-proven`), and rejects otherwise passing route-smoke JSON when the Vercel deployment binding is matched/observed but lacks `vercelProductionDeploymentEvidence.releaseRunIdStatus: "matched"` (`deployment-route-smoke-vercel-deployment-release-run-not-proven`). Older route-smoke evidence without these summaries is now stale for final approval.

Additional deployed UI/playback release-run binding guard note: deployed teacher page smoke, teacher workflow browser smoke, and deployed student PPT playback smoke now require their redacted `vercelProductionDeploymentEvidence.releaseRunIdStatus` binding to be `matched` before aggregate approval. The production gate rejects stale same-shape page evidence with `deployed-teacher-workflow-page-vercel-deployment-release-run-not-proven`, stale browser evidence with `teacher-workflow-browser-smoke-vercel-deployment-release-run-not-proven`, and stale student playback evidence with `deployed-learning-ppt-playback-vercel-deployment-release-run-not-proven`. This protects the Kang Xia student playback proof from reusing a matched/observed Vercel deployment binding from an older release attempt.

Additional Vercel env apply guard note: `scripts/vercel-env-sync.mjs --apply --approved` now runs a redacted preflight before any `vercel env add` mutation. It refuses to invoke the Vercel CLI unless the teacher-auth selector is accepted, selector-specific auth env is present, production-critical secret-strength checks are sufficient, external storage is remote HTTPS with a redacted service fingerprint, and OIDC endpoints are remote HTTPS when selected.

The local UAIS provider env for DeepSeek/Qwen is mostly present, the local website teacher workflow UI evidence is accepted and now includes explicit feature markers for uploaded 10-second audio payload submission via `uploadedSampleAudioPayload: true`, browser metadata duration gating via `voiceSampleDurationGate: true`, selected voice-sample identity propagation via `selectedSampleIdentity: true`, `/api/ai/session` signed-session bootstrap, `authFailClosed: true` handling when signed AI session minting is denied, `serverWorkflowStatus: true` for the `/api/ai/teacher-ppt-workflow` server-state bridge, step gating for the voice sample -> preflight -> voiceRef -> PPT WAV flow, an explicit Kang Xia owner-provided 10-second voice selector, and a Kang Xia 19-slide PPT narration path that displays 19 per-slide WAV download links in the component acceptance path. The aggregate gate rejects stale `accepted` teacher UI JSON unless `evidenceStatus` is `feature-evidence-passed` and `uploadedSampleAudioPayload`, `voiceSampleDurationGate`, `selectedSampleIdentity`, `authFailClosed`, and `serverWorkflowStatus` are present in the required feature proof. Local production build/start now proves `/teaching` renders the signed-session bootstrap marker, the server workflow readiness-progress marker, the orchestrated local production browser smoke proves short-upload duration gating, signed-session bootstrap, preflight, voiceRef, PPT narration submit, and 19 per-slide WAV links in a real browser, protected route smoke now passes with fixture env, the trusted teacher route-smoke contract now proves signed trusted issuer proof -> hardened issuer cookie with issuer-proof-bounded Max-Age -> teacher AI session with redacted `authProviderContract` present -> teacher ownership summary -> teacher PPT workflow locally, and `/api/ai/teacher-auth/issue` now has a tested OIDC/JWKS bearer-token path that caps cookie lifetime to token expiry and redacts token/provider details. The deployed page smoke and aggregate page gate now also require rendered voice select, uploaded audio payload marker, browser metadata duration-gate marker, selected sample identity marker, signed-session bootstrap marker, protected workflow action manifest, server workflow status marker, server workflow readiness-progress marker, redacted deployment fingerprint evidence, rendered-page fingerprint evidence from the fetched HTML, and remote-HTTPS deployment origin proof before any production page proof can pass. The aggregate gate now also requires a deployed browser interaction smoke after the deployed page is production/live/passed; that smoke must prove real browser hydration, fixture-intercepted workflow APIs, short uploaded audio gating via `voiceSampleDurationGate: "passed"`, uploaded voice file selection, signed-session request, protected workflow clicks, 19 per-slide WAV download links, matching deployment fingerprint, and no remote provider/storage mutation. The route-smoke harness now also has a redacted OIDC/JWKS live-token mode, but no real production OIDC smoke token has been supplied or run. The same local production diagnostic now runs against a localhost external storage reference service with both teacher ownership and Qwen lifecycle audit backends set to `external` and preserves route response-shape proof without storing response bodies. The production gate now requires owner-approved Vercel production deployment evidence from `target: "vercel-production-deployment"` before it can report ready, with live production deployed status, redacted deployment fingerprint, remote-HTTPS deployment origin, ready project-readiness guard proof, and redaction safety. The Vercel production deployment harness now refuses live deploy before Vercel CLI invocation unless ready project-readiness evidence is provided, and the aggregate gate rejects stale deployed evidence without `projectReadinessGuard: "proved"`. The Vercel production deployment fingerprint and deployed page-smoke fingerprint are now explicitly linked; if both are present but differ, the page requirement blocks with `vercel-production-deployment-fingerprint-mismatch` instead of accepting mixed-deployment evidence. Teacher auth/session provider readiness is now its own release proof: `teacher-auth-provider-readiness` must prove the selected production provider, sufficient teacher-auth session signing secret, hardened cookie contract, no-cookie-issued safety, and either trusted-issuer secret/TTL readiness or remote-HTTPS OIDC issuer/JWKS plus audience and teacher-id claim before route smoke can complete the chain. The aggregate gate also checks that Vercel env-sync, teacher-auth provider readiness, and protected route smoke agree on the same selected provider, blocking mixed-selector evidence with `teacher-auth-provider-selector-mismatch`. It also requires deployed page/browser/route smoke evidence to carry matching redacted deployment fingerprints and remote-HTTPS deployment origin proof, Vercel env apply evidence to come from the `vercel-env-sync` harness with ready project-readiness, redacted apply-summary counts, explicit selector-aware auth env proof, local-only smoke exclusion proof, remote-HTTPS external storage base URL proof, and remote-HTTPS OIDC endpoint class proof when `oidc-jwks` is selected. Production storage now has two separate release proofs: `external-storage-service-readiness` must first prove a real remote-HTTPS production service candidate with `production-service-mode-health-target`, API contract version, durable backing-store readiness, redacted health evidence, redacted service-origin fingerprint, and no-write semantics; `external-storage-smoke` must then prove same-run ownership and lifecycle audit write/read markers from a service with the same redacted fingerprint, otherwise `external-storage-service-consistency` blocks the final gate. A new redacted production E2E orchestration plan now machine-orders the final proof chain and records which steps mutate remote state or require owner approval; it does not replace the live evidence requirements. Vercel readiness now requires a redacted project-candidate check before project linking, and supports either exact project-name evidence or an owner-approved redacted project-id selector. The current Vercel scope has authenticated CLI/team evidence but no exact `uais` project candidate and no link marker. The file-backed external storage reference service has passed a localhost live smoke with redacted write/read verification for teacher ownership merge, Qwen lifecycle audit append, and same-run smoke marker readback, and it can now advertise the production service health target only via `--service-mode production` with a strong access token; this local mode still does not prove remote deployment or durable production storage. The Kang Xia narrated PPTX is ready for human playback QA in installed PowerPoint/WPS apps. The enterprise end-to-end production goal is still incomplete because approved Vercel project selection/linking, chosen and live-proven production teacher auth selector/env including either trusted issuer proof or OIDC/JWKS remote-HTTPS config, production durable external storage deployment/env with a remote-HTTPS base URL and production service identity, Vercel env apply with redacted apply-summary proof, owner-approved production deployment evidence, deployed URL HTML/browser/route smoke against a remote-HTTPS origin, a real approved OIDC live smoke-token run if `oidc-jwks` is selected, and actual manual PowerPoint/WPS playback acceptance remain unproven.
